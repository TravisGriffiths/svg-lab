---
export interface Props {
  innerHTML: unknown;
}

const { innerHTML, ...attributes } = Astro.props as Props;
// const { default: innerHTML } = await import(`${icon}.svg?raw`);
---

<Fragment {...attributes} set:html={innerHTML} />